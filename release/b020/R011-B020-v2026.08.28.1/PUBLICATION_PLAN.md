# R011-B020 offline packaging and publication handoff

The packaging scaffold packages only the exact 199-page B020 boundary-clean learner reader. Separate bounded publication entry points perform the authorized public transactions only when invoked with `--publish`; their self-check mode performs no credential access, Git invocation, network request, upload, publication, or upstream contact. The flow creates no competing Zenodo concept, never invokes local Git, and keeps both destinations public/open.

1. Promote the admitted PDF to `output/pdf/statistika-berbasis-data-batas-R011-B020.pdf`; it must remain 10207990 bytes with SHA-256 `edca0f9b731b43fe49e314b42bb0f6d2e4e5165ca26a8144a49d45c1111f10da` and exactly 199 pages.
2. Preserve the exact deterministic build, pagewise language, visual, and independent translation QA records and admit B020 into the modular backend. The backend manifest is validated dynamically at prepare time and must bind the exact reader SHA-256 and `R011-B020`.
3. Run `prepare_b020_release.py --prepare` to resolve the exact input identities, then `package_b020.py --package`. Both tools fail closed if the reader, any QA record, source snapshot, backend, or verified B019 predecessor changes.
4. Run `publish_zenodo_b020.py --publish`. It uses only the existing Zenodo concept `10.5281/zenodo.22059801`, creates one successor to public B019 record `22135360` / DOI `10.5281/zenodo.22135360`, keeps access open, and anonymously reads every released byte back.
5. After the sanitized Zenodo receipt exists, run `publish_github_b020.py --publish`. It builds one exact bounded REST-API tree on `KokunoYumeto/statistika-berbasis-data-id`, parented by B019 release commit `ab020b61fe13cb2b623d535d1ddfffe10aef6055`, tag `r011-b020-2026.08.28.1`, with no local or repository-wide Git scan; it anonymously verifies the exact public tree and every release asset.

Scope truth: partial; 199 accepted Indonesian learner-reader pages through Chapter 5 Section 5.2; exercises 1–14; upstream-public answers 1, 3, 5, 7, 9, 11, and 13; O001 gaps 2, 4, 6, 8, 10, 12, and 14; zero untranslated instructional/exercise prose in the learner reader; no restricted solutions; full source closure contains untranslated source and is not learner output. Next cursor: source line 1509, label `hypothesisTesting` at line 1510. Model: **OpenAI Codex gpt-5.6-sol, Ultra**. License: **CC BY-SA 3.0 Unported**, subject to component-specific rights.

