# R011-B015 publication checklist

This workflow remains inert until the separate guarded B015 admission and post-admission replay pass, every terminal identity is exact, and `RELEASE_INPUTS.json` says `READY_FOR_PACKAGING`. It targets only Zenodo concept `10.5281/zenodo.22059801`, fixed Figshare article `33314727` in project `280296` / collection `8668413`, and GitHub repository `KokunoYumeto/statistika-berbasis-data-id`. No upstream contact occurs.

1. Admission: independently validate the exact candidate reader, the deterministic localized `geometricDist70` PDF/R pair, source overlays, isolated B015 backend append, and all admission/control receipts. Promote only through the separate rollback-protected admission tool.
2. Bind: `python -B scripts/bind_release_inputs_b015.py --check`, then `--bind` only after admission is exact. The binder alone may replace the fixed unresolved contract.
3. Package: run `python -B scripts/package_release_b015.py --self-check`, then `--package`. The package is reader-first, deterministic, privacy-scanned, rights-aware, and excludes restricted witnesses, absolute-profile receipts, `.Rhistory`, caches, `earacupuncture.pdf`, and the unredistributed GPL-3 `openintro` package archive.
4. Zenodo: preserve the existing concept lineage after collision checks; upload the exact nine ordered assets, publish, and anonymously stream-read every byte.
5. Figshare: make exactly one B015 attempt against fixed article `33314727`. If the account/article route remains unavailable, write a truthful non-mutating terminal route-status receipt; never create a replacement, duplicate item, or follow-up retry for this release.
6. GitHub: require Zenodo plus the Figshare receipt/status, create an exact fresh tree from the bounded allowlist with the adaptive hierarchical tree algorithm, use the current branch head only as commit parent, create tag `r011-b015-2026.08.25.2`, attach the exact assets, and anonymously verify tree, raw bytes, and assets. No local Git command is part of this workflow.
7. Finalize: `python -B scripts/finalize_publication_b015.py --finalize` only after all public receipts prove anonymous byte readback. Advance the instructional cursor to `repo/ch_distributions/TeX/ch_distributions.tex#binomialModel` while keeping the corpus truthfully marked belum lengkap.

The accepted B015 scope is complete Section 4.2, *Distribusi geometrik*: 2 subsections, 3 examples, 3 guided exercises with 3 inline public answers, EoCE 11-16, public answers 11/13/15, O001 gaps 12/14/16, one localized data-appendix entry, and one deterministic localized PDF/R figure pair. Production model: **OpenAI Codex gpt-5.6-sol, Ultra**. Text, translation, and localized figure license: **CC BY-SA 3.0 Unported**, subject to component-specific rights.
