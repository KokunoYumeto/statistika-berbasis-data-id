# R011-B021 offline packaging and publication handoff

The packaging scaffold packages only the exact 216-page B021 boundary-clean learner reader. Separate bounded publication entry points perform the authorized public transactions only when invoked with `--publish`; their self-check mode performs no credential access, Git invocation, network request, upload, publication, or upstream contact. The flow creates no competing Zenodo concept, never invokes local Git, and keeps every destination public/open.

1. Promote the admitted PDF to `output/pdf/statistika-berbasis-data-batas-R011-B021.pdf`; it must remain 10377236 bytes with SHA-256 `3cc59f33d95fc832b14072845fcf48e058095a13d8b234079207d5781e1258e3` and exactly 216 pages.
2. Preserve the exact deterministic build, pagewise language JSON/TSV, whole-reader visual QA, five accepted source/target audit records, consolidated independent-translation verification receipt, and its exact verifier implementation; then admit B021 into the modular backend. The backend manifest is validated dynamically at prepare time and must bind the exact reader SHA-256 and `R011-B021`.
3. Run `prepare_b021_release.py --prepare` to resolve the exact input identities, then `package_b021.py --package`. Both tools fail closed if the reader, any QA record, source snapshot, backend, or verified B020 predecessor changes.
4. Run `publish_zenodo_b021.py --publish`. It uses only the existing Zenodo concept `10.5281/zenodo.22059801`, creates one successor to public B020 record `22142998` / DOI `10.5281/zenodo.22142998`, keeps access open, and anonymously reads every released byte back.
5. After the sanitized Zenodo receipt exists, run `publish_github_b021.py --publish`. It builds one exact bounded REST-API tree on `KokunoYumeto/statistika-berbasis-data-id`, parented by B020 release commit `73b78b32437f8b4eb69a719c3742a5cfa48be51d`, tag `r011-b021-2026.08.28.1`, with no local or repository-wide Git scan; it anonymously verifies the exact public tree and every release asset.
6. Retry only fixed Figshare article `33314727` in project `280296` and collection `8668413`. Never create a competing item. Host exact bytes only under an exact CC BY-SA 3.0 route; otherwise use a CC0 metadata/link-only item targeting the exact Zenodo bytes.

Scope truth: partial; 216 accepted Indonesian learner-reader pages through Chapter 5 Section 5.3; exercises 1–26; upstream-public odd answers 1–25; O001 gaps for even exercises 2–26; zero untranslated instructional/exercise/public-answer prose in the learner reader; no restricted solutions; full source closure contains untranslated source and is not learner output. Next cursor: Chapter 6 source line 29, label `singleProportion` at line 30. Model: **OpenAI Codex gpt-5.6-sol, Ultra**. License: **CC BY-SA 3.0 Unported**, subject to component-specific rights.
