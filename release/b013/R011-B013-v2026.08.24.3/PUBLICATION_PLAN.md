# R011-B013 publication checklist

This workflow remains inert until the separate guarded B013 admission and post-admission replay pass, every terminal identity is exact, and `RELEASE_INPUTS.json` says `READY_FOR_PACKAGING`. It targets only Zenodo concept `10.5281/zenodo.22059801`, fixed Figshare article `33314727` in project `280296` / collection `8668413`, and GitHub repository `KokunoYumeto/statistika-berbasis-data-id`. No upstream contact occurs.

1. Admission: independently validate the exact candidate reader, five localized PDFs, five source R producers, shared Python localizer, source overlays, isolated 4,516-record backend, and all admission/control receipts. Promote only through the separate rollback-protected admission tool.
2. Bind: `python -B scripts/bind_release_inputs_b013.py --check`, then `--bind` only after admission is exact. The binder alone may replace the fixed unresolved contract.
3. Package: run `python -B scripts/package_release_b013.py --self-check`, then `--package`. The package is reader-first, deterministic, privacy-scanned, rights-aware, and excludes restricted witnesses, absolute-profile receipts, `.Rhistory`, caches, and `earacupuncture.pdf`.
4. Zenodo: preserve the existing concept lineage after collision checks; upload the exact nine ordered assets, publish, and anonymously stream-read every byte.
5. Figshare: inspect/update only fixed article `33314727`. If the account/article route remains unavailable, write a truthful non-mutating route-status receipt; never create a replacement or duplicate item.
6. GitHub: require Zenodo plus the Figshare receipt/status, create an exact fresh tree from the bounded allowlist with the adaptive hierarchical tree algorithm, use the current branch head only as commit parent, create tag `r011-b013-2026.08.24.3`, attach the exact assets, and anonymously verify tree, raw bytes, and assets. No local Git command is part of this workflow.
7. Finalize: `python -B scripts/finalize_publication_b013.py --finalize` only after all public receipts prove anonymous byte readback. Advance the instructional cursor to `repo/ch_distributions/TeX/ch_distributions.tex#normalDist` while keeping the corpus truthfully marked belum lengkap.

The accepted B013 scope is Bagian 3.5 Distribusi kontinu: 3 contoh, 2 latihan terpandu beserta jawaban inline, EoCE 37-38, jawaban publik 37, O001 38, lima gambar PDF terlokalkan, lima produsen R sumber, dan satu pelokal Python bersama. Production model: **OpenAI Codex gpt-5.6-sol, Ultra**. Text/translation license: **CC BY-SA 3.0 Unported**, subject to component-specific rights.

